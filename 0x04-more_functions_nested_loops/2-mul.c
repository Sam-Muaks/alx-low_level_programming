#include "main.h"
#include <stdio.h>

/**
 * mul - takes two integers and multiplies them
 * @a: First integer
 * @b: Second integer
 * Return: Always 0.
 */
int mul(int a, int b)
{
	return (a * b);
}
