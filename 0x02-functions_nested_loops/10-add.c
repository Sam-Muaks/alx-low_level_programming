#include "main.h"
/**
 * add - function that adds two integers and returns the result
 * @one: first input integer
 * @two: second input integer
 * Return: one + two
 */
int add(int one, int two)
{
	return (one + two);
}
