#include <stdio.h>
/**
 * main - Entry point
 *
 * Return:Always 0 (Success)
 */
int main(void)
{
for (ch = 'a'; ch <= 'z'; ch++)
	putchar (ch);
	putchar ('\n');
	{
		printf("%c\n", ch);
	}
	return (0);
}
