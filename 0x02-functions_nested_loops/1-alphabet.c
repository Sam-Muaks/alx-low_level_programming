#include "main.h"
/**
 * print_alphabet - prints _putchar using print_alphabet prototype
 *
 * Return:Always 0 (Success)
 */
void print_alphabet(void)
{
char ch;
for (ch = 'a'; ch <= 'z'; ch++)
putchar (ch);
putchar ('\n');
}
