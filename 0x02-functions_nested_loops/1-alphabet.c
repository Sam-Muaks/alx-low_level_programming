#include <stdio.h>
/**
 * main - Entry point
 *
 * Return:Always 0 (Success)
 */
void print_alphabet(char);

int main()
{
char ch;
for (ch = 'a'; ch <= 'z'; ch++)
putchar (ch);
putchar ('\n');
	{
		printf("%c\n", ch);
	}
	return (0);
}
