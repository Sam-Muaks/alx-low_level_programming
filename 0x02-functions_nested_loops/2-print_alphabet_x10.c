#include "main.h"
/**
 * print_alphabet_x10 - function that prints 10 times the alphabet
 *
 */
void print_alphabet_x10(void)
{
	char ch;
	int line, ch
		while (line < 10)
		{
			for (ch = 'a'; ch <= 'z'; ch++)
				putchar(ch);
				putchar('\n');
		}
}
