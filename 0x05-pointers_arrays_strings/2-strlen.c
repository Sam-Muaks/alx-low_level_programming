#include "main.h"
/**
 * _strlen - returns the length of a character string
 *
 * @s: string input
 *
 * Return: length of the string
 */

int _strlen(char *s);
{
	char name[6] = "Mohri";
	int length = strlen(name);
}
